/**
 * agent-notes: { ctx: "P0 principal SDE, TDD green phase", deps: [], state: "canonical", last: "sato@2026-08-05" }
 */

export const transcriptionApi = {
  transcribe: async (fileUrl: string): Promise<string> => {
    // Basic stub implementation
    return '';
  }
};
